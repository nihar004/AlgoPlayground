{
  /* <motion.header
          initial={{ opacity: 0, y: -20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.5 }}
          className="mb-12 text-center"
        >
          <h1 className="text-4xl md:text-5xl font-bold mb-4 tracking-tight bg-clip-text text-transparent bg-gradient-to-r from-blue-500 to-indigo-600">
            Personalized DSA Learning Path
          </h1>
          <p className="text-lg text-zinc-600 dark:text-zinc-400 max-w-3xl mx-auto">
            Master Data Structures & Algorithms through our interactive learning
            paths. Each module includes video lectures, practice problems,
            visualizations, and carefully curated LeetCode challenges.
          </p>
        </motion.header> */
}

{
  /* <div className="mb-12 grid grid-cols-1 md:grid-cols-3 gap-8">
          {[
            {
              icon: <Video className="w-6 h-6 text-blue-500" />,
              title: "Video Lectures",
              description:
                "Learn concepts through comprehensive video tutorials with detailed explanations",
            },
            {
              icon: <Eye className="w-6 h-6 text-indigo-500" />,
              title: "Interactive Visualizations",
              description:
                "Understand algorithms through dynamic visualizations with custom inputs",
            },
            {
              icon: <Code className="w-6 h-6 text-purple-500" />,
              title: "Practice & Implementation",
              description:
                "Solidify learning with handpicked LeetCode problems and guided solutions",
            },
          ].map((item, index) => (
            <div
              key={index}
              className={`p-6 rounded-xl ${
                isDarkMode
                  ? "bg-zinc-800/50 border border-zinc-700"
                  : "bg-white border border-zinc-200"
              } shadow-lg`}
            >
              <div className="flex flex-col items-center text-center">
                <div
                  className={`w-12 h-12 rounded-full flex items-center justify-center mb-4 ${
                    isDarkMode ? "bg-zinc-700" : "bg-blue-50"
                  }`}
                >
                  {item.icon}
                </div>
                <h3
                  className={`text-lg font-semibold mb-2 ${
                    isDarkMode ? "text-white" : "text-zinc-800"
                  }`}
                >
                  {item.title}
                </h3>
                <p className="text-zinc-500 dark:text-zinc-400">
                  {item.description}
                </p>
              </div>
            </div>
          ))}
        </div> */
}
